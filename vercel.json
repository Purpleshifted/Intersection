{
  "buildCommand": "yarn build:vercel",
  "devCommand": "yarn dev",
  "installCommand": "yarn install",
  "framework": "nextjs",
  "regions": ["icn1"],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- noisecraft/ realtime/ || echo 'ignore'"
}

